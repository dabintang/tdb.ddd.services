数据表类型划分：
1、信息，info结尾；（如：用户信息）
2、记录，record结尾；（如：消费记录）
3、配置，config结尾；（如：角色权限配置）

字段类型划分：
1、编号，ID结尾，int/long；(一般是主键/外键)
2、编号，NO结尾，GUID；(一般是主键/外键)
3、编码，Code结尾，枚举/常量，tinyint；（如：性别 1-男，2-女）
4、是否XX，Is开头，bit；（如：是否已验证）
5、其他；